{
  "name": "simple-proxy-site",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build && cd ../server && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.12",
    "cheerio": "^1.0.0-rc.12",
    "helmet": "^7.0.0",
    "express-rate-limit": "^6.8.0"
  }
}
